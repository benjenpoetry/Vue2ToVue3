{
	"name": "@vv/application",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"serve": "vite --port=5173",
		"build": "vue-tsc -b && vite build",
		"preview": "vite preview",
        "deploy": "pm2 start npm --name vue2-to-vue3-5173 -- run preview"
	},
	"devDependencies": {
		"vite": "5.2.0"
	}
}
